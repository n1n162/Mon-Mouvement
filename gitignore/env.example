# Clés ORS (déjà en place)
ORS_API_KEY=your_ors_api_key

# Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID=price_...   # ID du produit créé dans le dashboard Stripe

# JWT — générer avec : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=une_chaine_aleatoire_tres_longue

# URL de l'app (sans slash final)
APP_URL=https://ton-app.vercel.app

# Admin — mot de passe pour générer des accès gratuits
ADMIN_PASSWORD=choisis_un_mot_de_passe_fort
